version: "1.0"

profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - node_modules
      - dist
      - .vite
      - "*.config.js"
      - "*.config.ts"
      - vite-env.d.ts

linter: jetbrains/qodana-js:2025.2